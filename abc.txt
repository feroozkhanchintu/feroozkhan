public void mapBatchSummaryData(List<BatchHeadTailVO> batchHeadTailVOs,
			ExceptionBatchProcessingDaoImpl daoImpl,
			Map<String, String> commonMap) {

		String userId = "AJANGA3";
		String fileName = commonMap.get("FN");
		String parentId = commonMap.get("PID");
		String checkSum = commonMap.get("CSUM");
		List<Integer> listBatchSummaryId = new ArrayList<Integer>();
		for (BatchHeadTailVO headTailVo : batchHeadTailVOs) {
			FirstDataExceptionBatchSummary exceptionBatchSummary = exceptionBatchSummary(userId, fileName, parentId,
					checkSum, headTailVo);
			// ExceptionDetail
			List<TranExceptionVO> exceptionList = headTailVo
					.getBatchExceptionList();
			if (exceptionList != null) {
				// exceptionBatchSummary.set
			}
			// ConfirmFraudDetail
			List<TranConfirmFraudVO> cfList = headTailVo
					.getBatchConfirmFraudList();
			if (cfList != null) {

			}
			daoImpl.save(exceptionBatchSummary);
			listBatchSummaryId.add(exceptionBatchSummary.getProcessedId());
			batchDetailSave(daoImpl, exceptionBatchSummary.getProcessedId(),
					headTailVo);
		}
		List<EPBatchDetailVO> batchDetailList = getBatchDetaisData(daoImpl,
				listBatchSummaryId);

		batchDetailProcessing(batchDetailList, daoImpl, checkSum);

	}
